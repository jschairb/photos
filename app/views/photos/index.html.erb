<h1>Photos</h1>

<%= link_to "New Photo", new_photo_path %>

<ul id="photos">
  <% @photos.each do |photo| -%>
    <li id="<%= dom_id(photo) %>"><%= link_to h(photo.title), photo_path(photo) %></li>
  <% end -%>
</ul>
